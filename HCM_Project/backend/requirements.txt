<div class="manage-section">
<label style="display:block; margin-bottom:4px;">VLAN / Network</label>
<select [(ngModel)]="selectedNetwork" style="width:100%; margin-bottom:8px;">
<option *ngFor="let n of vmNetworks" [value]="n.name">
{{ n.name }}{{ n.name === currentNetwork ? ' (current)' : '' }}
</option>
</select>
<button (click)="applyNetwork()" style="width:100%;">
Change Network
</button>
</div>
