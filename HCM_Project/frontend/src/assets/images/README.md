# Drop your custom images here

| Filename          | Used for                          | Required |
|-------------------|-----------------------------------|----------|
| `background.jpg`  | Login page full-bleed background  | Yes      |
| `logo.png`        | (Optional) company logo           | No       |

The blue cloud-in-hand image you uploaded should be saved as **`background.jpg`** in this directory.

The cloud icon shown on the login card is an inline SVG (no file needed).
